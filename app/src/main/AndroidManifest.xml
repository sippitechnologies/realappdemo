<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.complete.sensor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".maxusers"></activity>
        <activity android:name=".addinguser" />
        <activity android:name=".Register" />
        <activity android:name=".unregister_user" />
        <activity android:name=".confirmremv" />
        <activity android:name=".confirm" />
        <activity android:name=".done" />
        <activity android:name=".scan" />
        <activity android:name=".surety" />
        <activity android:name=".verificationfailed" />
        <activity android:name=".customverification" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>